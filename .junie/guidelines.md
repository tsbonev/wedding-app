# Junie Guidelines

- Every time a task is requested, run `npm run build` to check for errors.
- If any build errors are found, fix them immediately.
- Ensure the build passes before submitting the task.
